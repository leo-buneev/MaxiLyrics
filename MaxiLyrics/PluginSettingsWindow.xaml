<Window x:Class="MaxiLyrics.PluginSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PluginSettingsWindow" Loaded="Window_Loaded" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid Name="mainGrid" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <TextBlock Name="errorText" Foreground="DarkRed" Grid.Column="0" HorizontalAlignment="Stretch" />
            <Button Name="okButton" Margin="5,0,0,0" Width="45" Grid.Column="1" Content="OK" Click="okButton_Click" />
            <Button Name="cancelButton" Margin="5,0,0,0" Grid.Column="2" Content="Cancel" Click="cancelButton_Click" />
        </Grid>
    </Grid>
</Window>
